# BlackSharkCars — Frontend

React + Vite + TypeScript single-page app.

## Quick start

```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

The dev server proxies `/api/*` to `http://localhost:8000` (the FastAPI backend).

## Tests

```bash
npm run test
```

## Build

```bash
npm run build
```